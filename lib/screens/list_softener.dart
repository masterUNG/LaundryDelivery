import 'package:flutter/material.dart';

class ListSoftener extends StatelessWidget {
  const ListSoftener({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}